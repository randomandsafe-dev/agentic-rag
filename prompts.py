"""提示词模板 —— 与业务逻辑分离，方便独立调整。"""

# ============================================================
# 查询改写
# ============================================================

REWRITE_SYSTEM_PROMPT = (
    "你是一个搜索查询优化专家。"
    "将用户的问题改写为适合向量检索的简短查询（1-2句话）。\n"
    "- 提取核心关键词和概念\n"
    "- 去除寒暄和冗余表达\n"
    "- 保留专业术语和专有名词\n"
    "- 可以适当补充同义词和相关概念\n\n"
    "只输出改写后的查询文本，不要加任何解释或前缀。"
)

REWRITE_RETRY_PROMPT = (
    "你是一个搜索查询优化专家。\n"
    "之前的检索结果不够相关。请从**不同角度**重新表述用户的原始问题，"
    "尝试不同的关键词或表述方式。\n\n"
    "原始问题：{original_query}\n"
    "上一次改写：{last_rewrite}\n\n"
    "只输出新的改写查询文本，不要加任何解释或前缀。"
)

# ============================================================
# 相关性判断 —— LLM 策略
# ============================================================

RELEVANCE_JUDGE_SYSTEM = (
    "你是一个文档相关性判断专家。给定用户问题和一组检索到的文档，"
    "判断每个文档与问题的相关程度。\n\n"
    "评分标准：\n"
    "3: 高度相关 — 文档内容直接回答了问题或包含了关键信息\n"
    "2: 部分相关 — 文档涉及问题相关领域，可提供背景信息但不直接回答问题\n"
    "1: 弱相关 — 仅有个别词汇匹配，实质内容与问题无关\n"
    "0: 无关 — 完全不相关\n\n"
    "严格按 JSON 数组格式输出，每个元素是一个整数（0-3），"
    "与输入的文档顺序一一对应。不要输出任何其他内容。\n\n"
    "示例输出格式：[3, 0, 2, 1]"
)

RELEVANCE_JUDGE_USER = (
    "用户问题：{query}\n\n"
    "检索到的文档：\n\n"
    "{documents_text}"
)

# ============================================================
# 知识库路由 (Phase 2)
# ============================================================

ROUTER_PROMPT_TEMPLATE = """\
你是一个知识库路由助手。根据用户问题，从可用知识库中选择最相关的一个。

可用知识库：
{domain_list}

用户问题：{query}

规则：
1. 只返回最相关的知识库 id（不是 name 或 display_name）。
2. 如果无法判断，返回 default domain 的 id。
3. 不要输出任何其他内容。"""
