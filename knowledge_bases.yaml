# 知识库定义（向后兼容 — 新配置请使用 config/knowledge_bases.yaml）
domains:
  - id: default
    name: "默认知识库"
    description: "通用知识库，包含 data/ 目录下所有文档"
    data_dir: data
    persist_dir: chroma_db
    collection_name: knowledge_base
    default: true
    enabled: true
    keywords: []
